services:
  splitsafe-staging:
    container_name: splitsafe-staging
    build:
      context: ../..
      dockerfile: docker/stage/Dockerfile
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    env_file:
      - ../../.env
    restart: unless-stopped 